version: "3"
services:
    database:
        image: 'postgres:17-alpine'
        ports:
            - 5432:5432

        environment:
            POSTGRES_USER: laravel
            POSTGRES_PASSWORD: changeme
            POSTGRES_DB: eco_board
